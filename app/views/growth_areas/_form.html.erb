<%= form_for(@growth_area) do |f| %>
  <% if @growth_area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@growth_area.errors.count, "error") %> prohibited this growth_area from being saved:</h2>

      <ul>
      <% @growth_area.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
